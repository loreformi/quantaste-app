version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "4000:8000"
    volumes:
      - backend_data:/app/data
    environment:
      DATABASE_URL: "sqlite:///data/quantaste.db"
      DEFAULT_TICKERS: "AAPL,MSFT,GOOGL,AMZN,TSLA,NVDA"
    # depends_on:
    #   - db # Uncomment if using a separate database service like PostgreSQL

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "3001:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://backend:8000" # Communicate with backend service by its name
    depends_on:
      - backend

volumes:
  backend_data: